#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    /*
    cout << "a&b = " << (a & b) << endl;
    cout << "a|b =" << (a | b) << endl;
    cout << "a^b =" << (a ^ b) << endl;
    cout << "~a = " << (~a) << endl;

    cout << "a<<1 =" << (a << b) << endl;
    cout << "a>>1 =" << (a >> b) << endl;

    cout << a << " " << b << " ";

    for (int i = 1; i <= n; i++)
    {
        int nextNumber = a + b;
        cout << nextNumber << " ";
        a = b;
        b = nextNumber;
    }
    cout << endl;

    for (int i = 0; i <= n; i++)
    {
        cout << i << " ";
        if (i & i)
        {
            continue;
        }
        i++;
    }
    */
}