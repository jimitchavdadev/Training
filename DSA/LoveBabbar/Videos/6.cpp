#include <iostream>
#include <math.h>
using namespace std;
int main()
{   /*
    int n;
    cin >> n;
    int count = 0;
    while (n != 0)
    {
        if (n & 1)
        {
            count++;
        }
        n = n >> 1;
    }
    cout << count << endl;

    long ans = 0;
    int i = 0;
    while (n != 0)
    {
        int bit = n & 1;
        ans = bit * pow(10, i) + ans;
        n = n >> 1;
        i++;
    }
    cout << ans << endl;

    int ans = 0;
    int i = 0;
    while (n != 0)
    {
        int digit = n % 10;
        ans = digit * pow(2, i) + ans;
        i++;
        n = n / 10;
    }
    cout << ans << endl;
    */
}